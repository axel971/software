#ifndef LevelPyramide_h
#define LevelPyramide_h


class LevelPyramide {

 public:

  void build(cv::Mat image, double sigma, double k, int octave, int level, bool isFirstOctaveElmt = false);

    void setImage(cv::Mat image);

    cv::Mat getImage();


 private:
    int m_level;
    cv::Mat m_image;
    double m_sigma;

};

#endif // LevelPyramide_h
