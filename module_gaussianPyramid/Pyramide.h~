#ifndef Pyramide_h
#define Pyramide_h

#include <vector>

#include "LevelPyramide.h"


class Pyramide {

 public :

  //TO Do : Finish constructor
  Pyramide(int level, int octave, double sigma0, double k);
  void build();


 private: 
    int m_level;
    int m_octave;
    double m_sigma0;
    double m_k;


 private:

    /**
     * @element-type LevelPyramide
     */
    std::vector< LevelPyramide > data;

  
};

#endif // Pyramide_h
